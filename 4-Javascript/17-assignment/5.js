// Write a JavaScript function to generate a random number within a range(start, end).

const start = 2;
const end = 5;

function generateRandom(start, end) {
    console.log((Math.floor(Math.random() * end -1)) + start);
}

generateRandom(start, end);
generateRandom(start, end);
generateRandom(start, end);
generateRandom(start, end);
generateRandom(start, end);
generateRandom(start, end);
generateRandom(start, end);
generateRandom(start, end);
generateRandom(start, end);
generateRandom(start, end);
generateRandom(start, end);
generateRandom(start, end);

